version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - certbot-etc:/etc/letsencrypt
    restart: unless-stopped
    environment:
      - HOST=botdiscord.ru

volumes:
  certbot-etc:
